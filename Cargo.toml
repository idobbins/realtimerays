[package]
name = "realtimerays"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
png = "0.17"
serde_json = "1"

[profile.release]
panic = "abort"
strip = true
lto = "thin"
codegen-units = 1
