[package]
name = "realtimerays"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
ash = { version = "0.38.0", default-features = false, features = ["linked", "std"] }

[profile.release]
panic = "abort"
strip = true
lto = "thin"
codegen-units = 1
